# Customise this file, documentation can be found here:
# https://github.com/fastlane/fastlane/tree/master/fastlane/docs
# All available actions: https://github.com/fastlane/fastlane/blob/master/fastlane/docs/Actions.md
# can also be listed using the `fastlane actions` command

# Change the syntax highlighting to Ruby
# All lines starting with a # are ignored when running `fastlane`

# If you want to automatically update fastlane if a new version is available:
# update_fastlane

# This is the minimum version number required.
# Update this, if you use features of a newer version
fastlane_version "2.68.0"

default_platform :ios

ENV["FASTLANE_XCODEBUILD_SETTINGS_TIMEOUT"] = "120"

platform :ios do
  desc "Runs all the tests"
  lane :test do
    require "json"

    # Clone a private simulator per job so parallel Jenkins jobs never reset each
    # other's simulator. TEST_SOURCE_DEVICE + optional TEST_SOURCE_RUNTIME are an
    # explicit CI contract, resolved to exactly one UDID below.
    device  = ENV["TEST_SOURCE_DEVICE"]  || "iPhone 16"
    runtime = ENV["TEST_SOURCE_RUNTIME"]

    # Reclaim leaked clones, but keep the TTL longer than a test run so a sibling
    # job's still-running simulator is never deleted.
    clone_ttl = 2 * 60 * 60
    now = Time.now.to_i
    JSON.parse(sh("xcrun simctl list devices -j"))["devices"].each_value do |devices|
      devices.each do |d|
        next unless d["name"].start_with?("Ring-CI-")
        created = d["name"][/-(\d+)\z/, 1].to_i
        next if created != 0 && now - created < clone_ttl
        sh("xcrun simctl shutdown '#{d["udid"]}' 2>/dev/null || true")
        sh("xcrun simctl delete '#{d["udid"]}' 2>/dev/null || true")
      end
    end

    matches = JSON.parse(sh("xcrun simctl list devices available -j"))["devices"].flat_map do |rt, devices|
      next [] if runtime && !rt.include?(runtime)
      devices.select { |d| d["name"] == device }.map { |d| d["udid"] }
    end
    UI.user_error!("No available simulator matching '#{device}' #{runtime}") if matches.empty?
    UI.user_error!("Ambiguous source simulator '#{device}' #{runtime}: #{matches}") if matches.size > 1
    source_udid = matches.first

    job_id = (ENV["BUILD_TAG"] || ENV["BUILD_NUMBER"] || Process.pid).to_s.gsub(/[^A-Za-z0-9_-]/, "-")
    # Trailing "-<unix-time>" lets the reclaim loop age out leaked clones.
    clone_name = "Ring-CI-#{job_id}-#{now}"
    # simctl clone requires the source to be shut down.
    sh("xcrun simctl shutdown '#{source_udid}' 2>/dev/null || true")
    udid = sh("xcrun simctl clone '#{source_udid}' '#{clone_name}'").strip

    begin
      sh("xcrun simctl bootstatus '#{udid}' -b")
      run_tests(
        scheme: "Ring",
        destination: "platform=iOS Simulator,id=#{udid}",
        number_of_retries: 2,
        # Cap a single test so a network/DHT stall can't hang the job.
        xcargs: "-test-timeouts-enabled YES -default-test-execution-time-allowance 120"
      )
    ensure
      sh("xcrun simctl shutdown '#{udid}' 2>/dev/null || true")
      sh("xcrun simctl delete '#{udid}' 2>/dev/null || true")
    end
  end

  desc "Test build without packaging"
  lane :build do
    unlock_keychain
    match(
      force: true,
      type: "development"
    )
    gym(
      scheme: "Ring",
      clean: true,
      configuration: "Development",
      export_method: 'development',
      skip_codesigning: true,
      skip_package_ipa: true
    )
  end

  desc "Submit a new Beta Build to Apple TestFlight"
  lane :beta do
    unlock_keychain
    api_key = app_store_connect_api_key(
      key_id: "#{ENV["KEY_ID"]}",
      issuer_id: "#{ENV["ISSUER_ID"]}",
      key_filepath: "#{ENV["KEY_FILEPATH"]}",
      in_house: false,
    )
    match(
      api_key: api_key,
      app_identifier: ["com.savoirfairelinux.ring", "com.savoirfairelinux.ring.jamiNotificationExtension",
"com.savoirfairelinux.ring.jamiShareExtension"],
      type: "appstore",
      readonly: true
    )
    profile_path = ENV["sigh_com.savoirfairelinux.ring_appstore_profile-path"]
    profile_path_extension = ENV["sigh_com.savoirfairelinux.ring.jamiNotificationExtension_appstore_profile-path"]
    profile_path_share_extension = ENV["sigh_com.savoirfairelinux.ring.jamiShareExtension_appstore_profile-path"]
    update_project_provisioning(
       target_filter: "jamiNotificationExtension",
       build_configuration: "Release",
       profile: profile_path_extension
    )
    update_project_provisioning(
       target_filter: "jamiShareExtension",
       build_configuration: "Release",
       profile: profile_path_share_extension
    )
    update_project_provisioning(
       target_filter: "Ring",
       build_configuration: "Release",
       profile: profile_path
    )
    gym(
      scheme: "Ring",
      clean: true,
      configuration: "Release",
      export_method: 'app-store',
    )
    pilot(
      api_key: api_key,
      skip_waiting_for_build_processing: true,
    )
  end

  after_all do |lane|
    # This block is called, only if the executed lane was successful
  end

  error do |lane, exception|
    # This block is called, only if the executed lane has failed
  end
end
